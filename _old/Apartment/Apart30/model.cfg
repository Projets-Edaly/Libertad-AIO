class CfgSkeletons
{
	class Apartment_30_Skeleton
	{
		isDiscrete = 1;
		skeletonInherit="";
		skeletonBones[]=
		{
			"door_front", "",
			"door_1", "",
			"door_2", "",
			"door_3", "",
			"door_4", "",
			"door_5", "",
			"door_6", "",
			"door_7", "",
			"door_8", "",
			"door_9", "",
			"door_10", "",
			"door_11", "",
			"door_12", "",
			"door_13", "",
			"door_14", "",
			"door_15", "",
			"door_16", "",
			"door_17", "",
			"door_18", "",
			"door_19", "",
			"door_20", "",
			"door_21", "",
			"door_22", ""
		};
	};
	
};
class CfgModels
{
	class Apartment_30
	{
		sectionsInherit = "";
		sections[] = {};
		skeletonName = "Apartment_30_Skeleton";
		class Animations
		{
			class door_front_rot
			{
				type			= rotation;
				source			= Door_front_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_front; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_front_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};


			class door_1_rot
			{
				type			= rotation;
				source			= Door_1_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_1; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_1_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};


			class door_2_rot
			{
				type			= rotation;
				source			= Door_2_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_2; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_2_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};


			class door_3_rot
			{
				type			= rotation;
				source			= Door_3_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_3; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_3_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};


			class door_4_rot
			{
				type			= rotation;
				source			= Door_4_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_4; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_4_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_5_rot
			{
				type			= rotation;
				source			= Door_5_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_5; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_5_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_6_rot
			{
				type			= rotation;
				source			= Door_6_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_6; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_6_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_7_rot
			{
				type			= rotation;
				source			= Door_7_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_7; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_7_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_8_rot
			{
				type			= rotation;
				source			= Door_8_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_8; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_8_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_9_rot
			{
				type			= rotation;
				source			= Door_9_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_9; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_9_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_10_rot
			{
				type			= rotation;
				source			= Door_10_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_10; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_10_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_11_rot
			{
				type			= rotation;
				source			= Door_11_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_11; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_11_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_12_rot
			{
				type			= rotation;
				source			= Door_12_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_12; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_12_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_13_rot
			{
				type			= rotation;
				source			= Door_13_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_13; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_13_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_14_rot
			{
				type			= rotation;
				source			= Door_14_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_14; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_14_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_15_rot
			{
				type			= rotation;
				source			= Door_15_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_15; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_15_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_16_rot
			{
				type			= rotation;
				source			= Door_16_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_16; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_16_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_17_rot
			{
				type			= rotation;
				source			= Door_17_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_17; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_17_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_18_rot
			{
				type			= rotation;
				source			= Door_18_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_18; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_18_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_19_rot
			{
				type			= rotation;
				source			= Door_19_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_19; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_19_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};

			class door_20_rot
			{
				type			= rotation;
				source			= Door_20_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_20; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_20_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_21_rot
			{
				type			= rotation;
				source			= Door_21_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_21; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_21_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad 100); // "rad" converts from degrees to radians
			};

			class door_22_rot
			{
				type			= rotation;
				source			= Door_22_source; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_22; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= door_22_axis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				angle0			= 0;
				angle1			= (rad -100); // "rad" converts from degrees to radians
			};
		};
	};
};